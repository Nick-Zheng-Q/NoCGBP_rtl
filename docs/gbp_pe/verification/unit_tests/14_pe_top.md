# pe_top Unit Test (PE System Layer)

> **DEPRECATED**: `pe_top.sv` has been deleted. The PE is now organized as `gbp_pe.sv` which directly instantiates `noc_adapter` + 4 subsystems + `pull_server` + `neighbor_state_accumulator` + `writeback_controller`. See `13_gbp_pe.md` for the new top-level verification plan. This document is retained for reference only.
